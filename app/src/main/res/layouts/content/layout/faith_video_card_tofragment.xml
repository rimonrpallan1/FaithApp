<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/faith_video_play_grid_card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    card_view:cardCornerRadius="8dp"
    card_view:cardUseCompatPadding="true">

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="@dimen/_140"
        android:layout_height="@dimen/_100"
        android:padding="@dimen/_04">

        <!-- This for video thumbnail-->
        <ImageView
            android:id="@+id/faith_fragment_video_card_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:src="@drawable/sample_0" />

        <!-- This for video icon in center-->
        <ImageView
            android:id="@+id/faith_fragment_video_card_videoicon_center"
            android:src="@drawable/icon_play_overlay"
            android:layout_gravity="center"
            android:layout_width="50dp"
            android:layout_height="50dp"/>

    </FrameLayout>



</android.support.v7.widget.CardView>
